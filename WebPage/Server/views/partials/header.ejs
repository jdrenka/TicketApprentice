
<!-- Header for all ejs files-->
<div class="header">
    <a href= "/index" class="logo">CompanyLogo</a>
    <div class="header-right">
      <a href="/browse-events">Browse Events</a>
      <a href="/contactUs">Contact Us</a>
      <a href="/contactOrganizer">Contact Organizer</a>
      <% if (loggedIn) { %>
        <% if (organizer) { %>
          <a id = 'create_event' href="/createEvents">Create Event</a>
        <% } %>
        <div id = 'userInfo'>
        <a href="/user/profile">Logged in as :&nbsp;&nbsp; <%= username %></a>
        <a id = 'logout_button' href="/logout">Logout</a>
        </div>
      <% } else { %>
        <a href= "/signin.html" class="signin-register">Sign In / Register</a>
      <% } %>
    </div>
  </div>
  