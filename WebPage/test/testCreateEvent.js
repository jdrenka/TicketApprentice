/* 
This is a rough test file for the createEvents route
*/

const assert = require('assert');
const { JSDOM } = require('jsdom');
const fs = require('fs');

describe('POST /createEvents', () => {
    it('should create a new event with valid form data', (done) => {
        //todo
    });

    it('should return an error with invalid form data', (done) => {
       //todo
    });
});



